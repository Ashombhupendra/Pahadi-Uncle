<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".presentation.notifications.NotificationFragment">
     <com.google.android.material.progressindicator.LinearProgressIndicator
         android:id="@+id/notification_progress_indicator"
         android:layout_width="match_parent"
         android:layout_height="wrap_content"
         android:indeterminate="true"
         app:indicatorColor="@color/orange_dark"
         app:layout_constraintTop_toTopOf="parent"
         app:trackColor="@android:color/white" />
     <androidx.recyclerview.widget.RecyclerView
         android:id="@+id/rv_notification"
         android:layout_width="match_parent"
         android:padding="10dp"
         android:layoutAnimation="@anim/layout_animation"
         tools:listitem="@layout/notification_item"
         android:layout_height="wrap_content"/>
     <TextView
         android:id="@+id/notification_no_found"
         android:text="No Notification were found. "
         android:layout_width="match_parent"
         android:layout_marginTop="20dp"
         android:textAlignment="center"
         android:textSize="20sp"
         android:visibility="gone"
         android:layout_height="wrap_content"
         />
</FrameLayout>